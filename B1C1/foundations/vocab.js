//  ? variable
const variable1 = "value"
// value can be 

// ? array
const variables = ["inside square brackets", "strings", variable1, 2, 3.2, "empty" ]
const emptyVariableArrays = []
// You can either declare a new array as empty, or you can provide it with initial values upon declaration
// variable still has a single value

// index starts at 0
const variable = variables[4]
console.log(variable)
// retrieves only the 5th item in the array


// ? string

// ? value

// ? console log
console.log('you can hold a random string in a console log ')
//or
console.log(variable)
//or
console.log(variable, variables)
// or 
console.log( `you can have a ${variable1} inside of a string`)
// ! do backtick not parenthesis

