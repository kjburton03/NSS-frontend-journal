const variable = "value"

const variables = [ "string", "value", "assign an array as a value of a variable", 4, 3, 4]
// use a plural variable for an array 

const boogyMan = [ "Clay", "Clay", "Clay", "Clay" ];
const toFireInKilnDEATHHHHHH = [];

for (const tacoTruck of boogyMan) {
   const penis = `${tacoTruck} coffee mug`;
   toFireInKilnDEATHHHHHH.push(penis);
}

console.log(toFireInKilnDEATHHHHHH);

const clay = [ "Clay", "Clay", "Clay", "Clay" ]
const toFireInKiln = []

for (clayValue of clay) {
   const mug = `${clayValue} coffee mug`
   toFireInKiln.push(mug)
}

console.log(toFireInKiln)